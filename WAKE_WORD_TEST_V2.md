# 唤醒词测试模式 V2 - 数字播报功能

## 🎉 新功能

基于之前的测试模式，现在添加了**概率数字播报**功能！

### **V1（之前）**

- 低音提示 → 说唤醒词 → 高音提示 → **只在日志显示概率** → 等待 2 秒 → 循环

### **V2（现在）** ✅

- 低音提示 → 说唤醒词 → 高音提示 → **📢 播报概率数字** → 等待 5 秒 → 循环

---

## 🔊 **数字播报功能**

### **工作原理**

使用设备内置的数字音频文件（0-9），将概率转换为数字字符串并逐个播放。

### **示例**

| 概率 | 转换 | 播报内容 |
|------|------|---------|
| 0.567 | "0567" | "零五六七" |
| 0.523 | "0523" | "零五二三" |
| 0.489 | "0489" | "零四八九" |
| 0.601 | "0601" | "零六零一" |

### **特点**

- ✅ 无需小数点，直接读 4 位数字
- ✅ 每个数字间隔 600ms，清晰易听
- ✅ 使用内置音频，无需网络/TTS
- ✅ 边测试边听，无需盯着屏幕

---

## 📝 **代码变更**

### **新增函数：`SpeakProbability(float probability)`**

```cpp
/**
 * @brief 播报概率数字（使用内置数字音频）
 * @param probability 概率值（0.0-1.0），例如 0.567
 * 
 * 将概率转换为数字字符串并逐个播放，例如：
 * - 0.567 → "0567" → 读作 "零五六七"
 * - 0.523 → "0523" → 读作 "零五二三"
 */
void Application::SpeakProbability(float probability) {
    // 转换为 4 位数字
    int prob_int = (int)(probability * 1000);  // 0.567 → 567
    char prob_str[5];
    snprintf(prob_str, sizeof(prob_str), "%04d", prob_int);  // "0567"
    
    // 逐个播放数字
    for (size_t i = 0; i < strlen(prob_str); i++) {
        char digit = prob_str[i];
        // 播放对应的数字音频（0.ogg - 9.ogg）
        audio_service_.PlaySound(digit_sounds[digit]);
        vTaskDelay(pdMS_TO_TICKS(600));  // 间隔 600ms
    }
}
```

### **集成到测试模式**

在 `OnWakeWordDetectedInTestMode()` 中调用：

```cpp
// 播放高音提示音后...
vTaskDelay(pdMS_TO_TICKS(500));

// 🔊 播报概率数字
ESP_LOGI(TAG, "🔊 Speaking probability digits...");
SpeakProbability(last_wake_word_probability_);

// 等待冷却时间（5秒）
ESP_LOGI(TAG, "⏳ Cooling down for 5 seconds...");
vTaskDelay(pdMS_TO_TICKS(5000));
```

---

## 🎯 **使用体验**

### **之前（V1）**

1. 听到低音 → 说唤醒词
2. 听到高音 → **看日志** 查概率
3. **记录数据** → 手动输入到表格
4. 2 秒后继续

**问题**：需要一直看着屏幕

### **现在（V2）**

1. 听到低音 → 说唤醒词
2. 听到高音 → **听到** "零五六七"
3. **脑中记忆或口述** → 边听边记录
4. 5 秒后继续

**优势**：可以闭着眼睛测试！👍

---

## ⏱️ **时间轴对比**

### **V1（2 秒冷却）**

```
0s: 低音 → 等待用户
Xs: 检测到 → 高音
X+0.3s: 日志显示概率
X+2.3s: 下一轮开始
```

**每轮最短**：~2.5 秒

### **V2（5 秒冷却 + 数字播报）**

```
0s: 低音 → 等待用户
Xs: 检测到 → 高音
X+0.5s: 播报 "零" (0)
X+1.1s: 播报 "五" (5)
X+1.7s: 播报 "六" (6)
X+2.3s: 播报 "七" (7)
X+2.9s: 播报完成
X+7.9s: 下一轮开始
```

**每轮最短**：~8 秒  
**数字播报时间**：~2.4 秒（4 个数字）

---

## 🎨 **音频体验**

### **完整测试循环音频效果**

```
"嘟~" (500Hz 低音)
    [你说: "Hey Ploud"]
"嘀嘀~" (1000Hz + 1200Hz 高音)
"零" (女声/男声数字)
"五"
"六"
"七"
    [5 秒静默]
"嘟~" (再次循环)
```

---

## 🔧 **配置参数**

### **可调整的参数**

| 参数 | 位置 | 默认值 | 说明 |
|------|------|-------|------|
| 数字间隔 | `SpeakProbability` | 600ms | 每个数字播放后的间隔 |
| 冷却时间 | `OnWakeWordDetectedInTestMode` | 5000ms | 数字播报后的冷却时间 |
| 低音频率 | `StartWakeWordTestCycle` | 500Hz | 准备提示音 |
| 高音频率 | `OnWakeWordDetectedInTestMode` | 1000/1200Hz | 成功提示音 |

### **快速调整**

**加快测试速度**（缩短冷却时间）：
```cpp
vTaskDelay(pdMS_TO_TICKS(3000));  // 5秒 → 3秒
```

**加快数字播报**（缩短间隔）：
```cpp
vTaskDelay(pdMS_TO_TICKS(400));  // 600ms → 400ms
```

**禁用数字播报**（只保留日志）：
```cpp
// ESP_LOGI(TAG, "🔊 Speaking probability digits...");
// SpeakProbability(last_wake_word_probability_);
```

---

## 📊 **测试效率对比**

### **测试 50 次所需时间**

| 版本 | 每轮时间 | 50 次总时间 | 备注 |
|------|---------|------------|------|
| **V1** | ~3 秒 | ~2.5 分钟 | 需要看屏幕 |
| **V2** | ~9 秒 | ~7.5 分钟 | 可以闭眼听 |

**虽然 V2 慢了 3 倍，但是：**
- ✅ 解放双眼，不用盯屏幕
- ✅ 可以边做其他事边测试
- ✅ 听觉记忆更准确
- ✅ 减少视觉疲劳

---

## 🎯 **最佳实践**

### **推荐测试方式**

1. **准备录音工具**（可选）
   - 手机录音，事后整理数据
   - 或边听边手写记录

2. **进入测试状态**
   - 找个安静的地方
   - 戴上耳机听提示音更清楚
   - 准备好纸笔

3. **开始测试**
   - 听到低音 → 说唤醒词
   - 听到数字 → 快速记下或录音
   - 等待 5 秒 → 重复

4. **数据整理**
   - 听录音或查看笔记
   - 整理到表格
   - 分析概率分布

---

## 📝 **测试记录表（升级版）**

| # | 时间 | 唤醒词 | 听到数字 | 日志概率 | 成功 | 备注 |
|---|------|-------|---------|---------|------|------|
| 1 | 10:23 | hey ploud | 0567 | 0.567 | ✅ | 清晰 |
| 2 | 10:24 | hey ploud | 0523 | 0.523 | ✅ | |
| 3 | 10:24 | hey ploud | (无) | 0.489 | ❌ | 未触发 |
| 4 | 10:25 | hey ploud | 0601 | 0.601 | ✅ | |

**提示**：
- "听到数字"列可以边听边填
- "日志概率"列事后对照日志确认
- 两列应该一致（验证听力准确性）

---

## 🆚 **V1 vs V2 总结**

| 特性 | V1 | V2 |
|------|----|----|
| **概率反馈** | 仅日志 | 日志 + 语音 ✅ |
| **用户体验** | 需要看屏幕 | 可以闭眼 ✅ |
| **测试速度** | 快（3秒/轮）| 慢（9秒/轮）|
| **数据记录** | 手动看屏幕 | 听觉记忆 ✅ |
| **冷却时间** | 2 秒 | 5 秒 |
| **疲劳度** | 高（视觉）| 低（听觉）✅ |
| **推荐场景** | 快速验证 | 长时间测试 ✅ |

---

## 🚀 **立即试用**

### **步骤 1：确认已启用测试模式**

在 `main/application.cc` 的 `Application::Start()` 中：

```cpp
vTaskDelay(pdMS_TO_TICKS(2000));
EnableWakeWordTestMode(true);  // ← 确认这行存在
```

### **步骤 2：编译烧录**

```bash
cd /Users/xionghao/Documents/plaud/GitHub/xiaozhi-esp32
idf.py build flash monitor
```

### **步骤 3：体验新功能**

1. 听到 "嘟~" → 说 "Hey Ploud"
2. 听到 "嘀嘀~" → 注意听数字
3. 听到 "零五六七" → 记下 0.567
4. 等 5 秒 → 自动下一轮

---

## 💡 **使用技巧**

### **技巧 1：边听边记**

不要等数字播完才记录，听到第一个数字就开始写：
- 听到 "零" → 写 `0`
- 听到 "五" → 写 `5`
- ...

### **技巧 2：用录音笔**

开启手机录音，事后慢慢整理：
- 录下所有测试过程
- 听录音时暂停/倒带
- 准确记录每次概率

### **技巧 3：口述记录**

自己说出听到的数字（录音会录下你的声音）：
- 听到 "零五六七" → 说 "第1次，567"
- 听到 "零五二三" → 说 "第2次，523"

---

## ✅ **功能清单**

- [x] ✅ 播放低音准备提示（500Hz）
- [x] ✅ 检测唤醒词
- [x] ✅ 播放高音成功提示（1000Hz + 1200Hz）
- [x] ✅ **播报概率数字（0-9 音频）** ← 新增！
- [x] ✅ 日志显示详细信息
- [x] ✅ 自动循环（5 秒冷却）
- [x] ✅ 可配置参数（间隔、冷却时间）

---

## 🎉 **总结**

**V2 版本的唤醒词测试模式现在更加人性化！**

- 🔊 **听得到**：概率通过语音播报
- 👂 **记得住**：听觉记忆比视觉更持久
- 😌 **不累眼**：不用一直盯屏幕
- 📊 **好记录**：边听边记或录音整理

**开始你的测试之旅吧！** 🎤

---

**相关文档**：
- `WAKE_WORD_TEST_EXAMPLE.md` - 快速入门
- `docs/xiaozhi-esp32/audio/wake-word-test-mode.md` - 完整指南
- `docs/xiaozhi-esp32/audio/hey-ploud-model-integration.md` - 模型集成

